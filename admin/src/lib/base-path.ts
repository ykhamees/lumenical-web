// Next's basePath (next.config.mjs) automatically prefixes page routing and
// next/link, but NOT plain fetch() calls — those need this added manually.
// Must stay in sync with next.config.mjs's basePath value.
export const BASE_PATH = "/website";
